---
- name: include install tasks
  ansible.builtin.include_tasks: "install-{{ wsdd_install_from }}.yaml"
  tags:
    - wsdd

- name: allow firewall
  community.general.ufw:
    rule: allow
    name: wsdd
  become: true
